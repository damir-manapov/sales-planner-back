# Server
PORT=3000

# Database - Use either DATABASE_URL (for Neon/remote) or individual params (for local)

# Neon connection string (get from Neon dashboard)
# DATABASE_URL=postgres://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# Local Postgres (used if DATABASE_URL is not set)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=sales_planner
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
